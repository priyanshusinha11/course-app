import Button from '@mui/material/Button';
import TextField from "@mui/material/TextField";
import { Card, Typography } from "@mui/material";

function Signup() {
    return <div>
        <div>
            <Typography>
                Welcome back. Signup below
            </Typography>
        </div>
    </div>
}

export default Signup;
